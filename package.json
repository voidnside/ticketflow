{
  "name": "ticketflow",
  "version": "1.0.0",
  "description": "REST API simulating ticket lifecycle management with status tracking and SLA fields",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
